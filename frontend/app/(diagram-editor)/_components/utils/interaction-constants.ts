/**
 * Constants for interaction handling
 */

// Minimum distance (in flow coordinates) to consider mouse as moved
export const MIN_MOVE_DISTANCE = 5;

// Zoom factor for wheel zoom
export const ZOOM_FACTOR = 0.1;

// Zoom limits
export const MIN_ZOOM = 0.1;
export const MAX_ZOOM = 2;
